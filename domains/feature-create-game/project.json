{
  "name": "feature-create-game",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "domains/feature-create-game/src",
  "projectType": "library",
  "tags": ["ts", "logic", "factory"],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "domains/feature-create-game/jest.config.ts"
      }
    }
  }
}
